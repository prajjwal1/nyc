# UI review

No UI change proposed. The highest-priority actionable directive is ranking-signal quality, and the current frontend builds cleanly. Map view (fb-205) remains deferred until coordinate coverage is high enough to support it.
