# Feedback for this run — 2026-08-21-1622

No new product feedback beyond the `/self-improve` invocation.

## Question gate

Closed. The newest user-explicit backlog item is older than seven days, but more than three open items remain, so the protocol does not interrupt this run with a calibration question.

## Top directives

1. **fb-195 — validate/retire hard-coded ranking keywords.** Address this round with a measured, conservative reduction of the legacy positive-keyword layer.
2. **fb-138 — Reddit harvesting.** Deferred: the code fallback already shipped; the remaining OAuth setup needs user credentials/action.
3. **fb-158 — empty generic URLs.** Deferred: destructive source pruning requires fresh per-URL probes and user opt-in under the additive-only rule.

Related item **fb-199** is partially addressed by retiring redundant zero-yield positive rules, but broad snapshot-only deletion remains too risky without explicit engagement negatives.
